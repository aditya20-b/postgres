# Copyright (c) 2024, PostgreSQL Global Development Group (or your name/org)
#
# This file is part of the PostgreSQL extensibility showcase.
#

sources = files('blockchainam.c')

# The sources listed here will be picked up by the parent meson.build
# in src/backend/access/ when subdir('blockchain') is called.
# No need to define a separate library here if blockchainam.c is to be
# compiled as part of the main backend or a larger access method library.
